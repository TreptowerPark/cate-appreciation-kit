# Leaf streaming React example

`App.tsx` shows one live AI row subscribing to one `streamId`. Stable rows are memoized and do not rerender for every token.

Use this pattern in a canvas IDE when chat panels, code views, terminals, and webviews share one screen.
